# Models folder

This folder is synced using `git submodule` feature. It's shared between front and backend.

This model require parent project to install `npm install zod` with at least v4.

Models export ZOD object, typescript type and async function to query using fetch, with automatic typing and default configuration for cors, cache, ...
